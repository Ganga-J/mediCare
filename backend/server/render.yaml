services:
  - type: web
    name: medicare-backend
    runtime: node
    buildCommand: npm ci --omit=dev
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      # Add other env vars as needed (DB_URL, JWT_SECRET, etc.)